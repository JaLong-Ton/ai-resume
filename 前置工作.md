# AI 智能简历分析系统 — 前置工作清单

## 一、需求文档 vs 现有项目 的差异分析

| 维度 | 需求文档要求 | 现有 ResumeScreening 项目 | 差距 |
|------|-------------|--------------------------|------|
| 运行环境 | **阿里云函数计算 FC (Serverless)** | Docker + 常驻服务进程 | 架构完全不兼容，需重构 |
| API 规范 | RESTful，无认证要求 | JWT + RBAC 三级权限 | 认证体系需要移除或简化 |
| 简历格式 | **仅 PDF** | PDF + DOCX + DOC | 功能范围缩小 |
| 信息提取 | 必须：姓名/电话/邮箱/地址；加分：求职意向/薪资/工作年限/学历/项目经历 | 20+ 字段（含人脸检测） | 需求更精简，但字段集不同 |
| 评分匹配 | 岗位需求关键词提取 + 匹配度评分 | LLM 语义筛选 + LangGraph 工作流 | 需求强调"匹配度评分"而非"是否合格" |
| 缓存 | 加分项：Redis | Redis（内建） | 可简化 |
| 前端 | **必须部署到 GitHub Pages** | 前端在同一 Docker 容器内 | 前后端需要彻底分离 |
| 时限 | **24 小时内提交** | 成熟开源项目 | 时间非常紧张 |

---

## 二、必须的前置准备（按优先级排序）

### 第一优先级：账号与密钥（必须提前搞定，否则无法开始）

| 序号 | 事项 | 说明 | 预估耗时 |
|------|------|------|----------|
| 1 | **阿里云账号 + 函数计算 FC 开通** | 需求规定运行在阿里云 Serverless。需要实名认证、开通 FC 服务、了解部署方式（CLI/控制台/ROS） | 1-2 小时 |
| 2 | **DeepSeek API Key** | 用于 LLM 调用，解析简历文本、提取信息、评分匹配。需充值（价格低廉，5-10 元即可） | 10 分钟 |
| 3 | ~~DashScope API Key~~ | 需求文档未要求向量检索，现有项目使用的 Embedding 服务可**暂不申请**。如需"加分项-更精准的 AI 评分"，再补充 | 可选 |
| 4 | **GitHub 账号 + Pages 服务确认** | 前端部署到 GitHub Pages。确认账号可用，了解 Pages 部署流程 | 10 分钟 |

### 第二优先级：技术方案决策

| 序号 | 事项 | 选项 | 建议 |
|------|------|------|------|
| 5 | **Serverless 适配方案** | A) 直接用 FastAPI + 阿里云 FC 的 HTTP 触发器（Custom Runtime）  B) 拆成多个云函数，每个接口一个函数 | 建议方案 A，改动最小，24 小时内可行 |
| 6 | **PDF 解析库** | A) PyMuPDF (fitz) — 现有项目使用  B) pdfplumber — 更轻量 | 建议 PyMuPDF，解析效果好，支持多页 |
| 7 | **数据库方案** | A) 阿里云 RDS MySQL（Serverless 场景下可能冷启动慢）  B) SQLite + 阿里云 NAS/OSS 挂载  C) 不做持久化，仅内存处理 | 建议方案 C 或 B。需求未明确要求数据持久化，24 小时内优先保证功能跑通 |
| 8 | **前端技术栈** | A) 复用现有项目的原生 HTML/CSS/JS（约 2000+ 行）  B) 从零写一个更简洁的单页 | 建议方案 A，复用现有前端的登录/上传/结果展示页面并精简 |
| 9 | **缓存方案** | A) Redis（需申请阿里云 Redis 实例，耗时）  B) 函数计算内置的临时缓存  C) 跳过缓存（非必须项） | 建议方案 C 先跳过，时间充裕再加 |

### 第三优先级：开发环境与工具

| 序号 | 事项 | 说明 |
|------|------|------|
| 10 | **Python 3.10-3.12 环境** | 现有项目使用 `uv` 管理依赖。直接用 `uv` 可复现环境 |
| 11 | **Postman / Bruno / curl** | 用于测试 API。必选 |
| 12 | **VS Code + Python 插件** | 开发编辑器 |
| 13 | **Git 初始化** | 需求评分标准有"Git 提交规范"，需要从第一行代码就开始 commit |

---

## 三、推荐的最小可行方案（24 小时路线图）

考虑到 24 小时的限制，建议**大幅简化**，做到"刚好满足评分标准"：

### 后端架构：单一 FastAPI 云函数

```
app.py                  # FastAPI 入口，适配阿里云 FC HTTP 触发器
├── POST /upload        # 上传 PDF，返回解析+评分结果
├── POST /match         # 输入岗位描述 + 简历文本，返回匹配度
└── GET  /health        # 健康检查
```

- 不使用 MySQL / Redis / MinIO / ChromaDB / LangGraph
- 不使用认证 (需求未要求)
- PDF 上传后即时处理并返回 JSON，不做持久化存储
- 仅依赖：`fastapi` + `pymupdf` + `openai`（兼容 DeepSeek API）

### 前端：单页 HTML

- 一个 HTML 文件 + CSS + JS，部署到 GitHub Pages
- 功能：上传 PDF → 展示解析结果 + 评分
- 调用后端的阿里云 FC HTTP 地址

### 关键简化决策

| 现有项目组件 | 处理方式 | 理由 |
|-------------|----------|------|
| LangChain / LangGraph | **移除** | 需求未要求工作流编排，直接调用 DeepSeek API 即可 |
| MySQL / ChromaDB | **移除** | 需求未要求数据持久化，即时返回结果即可 |
| Redis | **移除** | 加分项，时间不够先跳过 |
| JWT / RBAC | **移除** | 需求未提认证要求 |
| WebSocket | **移除** | 需求未要求实时推送 |
| MinIO | **移除** | 需求仅要求 PDF 文本提取，不要求图片 |
| 人脸检测 (OpenCV) | **移除** | 需求未要求 |
| LangGraph 状态机 | **移除** | 流程简化为：解析 → 提取 → 评分 → 返回 |

---

## 四、前置工作检查清单

在开始写代码之前，确认以下事项已就绪：

- [ ] 阿里云账号已实名，函数计算 FC 服务已开通
- [ ] 已了解 FC 部署方式（推荐 CLI: `fun` 或 `s` 工具）
- [ ] DeepSeek API Key 已获取并充值
- [ ] GitHub 仓库已创建，Pages 服务已确认
- [ ] Python 3.10+ 环境已安装
- [ ] `uv` 包管理器已安装
- [ ] 已准备 3-5 份测试用 PDF 简历（中英文各若干）
- [ ] 已准备好岗位描述文本（用于测试匹配评分）
- [ ] Git 已初始化，准备好写 commit message

---

## 五、风险提示

1. **阿里云 FC 冷启动延迟**：Python + FastAPI 冷启动可能 500ms-2s。如果评分标准注重性能，需考虑预留实例或使用 Custom Container 方案。
2. **DeepSeek API 调用延迟**：每次 LLM 调用约 1-3 秒，单份简历可能需要 2 次调用（提取 + 评分），总计 3-6 秒。
3. **PDF 解析兼容性**：中文 PDF 可能出现编码问题、双栏布局解析混乱。需要提前准备多种格式的测试简历。
4. **阿里云 FC 文件上传限制**：FC 的 HTTP 触发器对请求体大小有限制（默认 6MB），需确认 PDF 文件大小上限。
5. **GitHub Pages CORS**：前后端分离部署时，需在后端配置 CORS 允许 GitHub Pages 域名访问。
